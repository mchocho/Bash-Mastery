#!/bin/bash

# Tilde expansion allows us to easily refer to the current
# user's home directory
#      echo ~
#      echo ~root

#      echo $PWD	 displays current directory
#      echo ~+           a shorthand for $PWD

#      echo $OLDPWD      displays previous directory before cd
#      echo ~-           a shorthand for $OLDPWD

# To continuously change between current directory and previous
# directory you can run
#      cd ~-

exit 0
